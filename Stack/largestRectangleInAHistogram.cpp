#include<iostream>
using namespace std;

//given an array.Each element represents the height to the histogram
//bar and the width of each bar is 1,find the read of largest rectangle in the histogram.


//brute force

int main(){
    int n;
    int arr[n];
    for(int i=0;i<n;i++){
        
    }
    return 0;
}